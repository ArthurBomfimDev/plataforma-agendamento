using Booking.Domain.Base;

namespace Booking.Domain.Module.Catalog;

// VIOLAÇÃO PROPOSITAL — prova de que o teste de fronteira quebra o build. Removida no commit seguinte.
public sealed class BoundaryProbeItem : BaseEntity;
