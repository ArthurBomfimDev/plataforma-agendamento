namespace AutoMapper;

/// <summary>
/// Imitação com o mesmo namespace e nome do tipo do AutoMapper, só para plantar a dependência da
/// decisão 6 sem instalar o pacote — instalar o pacote real reprovaria, com razão, o teste de pacotes.
/// </summary>
public interface IMapper
{
}
